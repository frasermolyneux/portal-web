@using System.Text.Json
@using XtremeIdiots.Portal.Web.Extensions
@using XtremeIdiots.Portal.Repository.Abstractions.Models.V1.BanFileMonitors
@model BanFileMonitorDto

@{
    ViewData["Title"] = "Ban File Monitor Details";
    var serverConfigs = ViewBag.ServerConfigs as Dictionary<Guid, Dictionary<string, JsonElement>>;
}

@functions {
    static string GetCfg(Dictionary<Guid, Dictionary<string, JsonElement>>? configs, Guid id, string ns, string prop)
    {
        if (configs != null &&
            configs.TryGetValue(id, out var nc) &&
            nc.TryGetValue(ns, out var r) &&
            r.TryGetProperty(prop, out var v) &&
            v.ValueKind == JsonValueKind.String)
            return v.GetString() ?? "";
        return "";
    }

    static int GetCfgInt(Dictionary<Guid, Dictionary<string, JsonElement>>? configs, Guid id, string ns, string prop, int def = 0)
    {
        if (configs != null &&
            configs.TryGetValue(id, out var nc) &&
            nc.TryGetValue(ns, out var r) &&
            r.TryGetProperty(prop, out var v) &&
            v.ValueKind == JsonValueKind.Number)
            return v.GetInt32();
        return def;
    }
}

<div class="wrapper wrapper-content animated fadeInRight">

    <div class="row">
        <div class="col-12">

            <div class="container-fluid">
                <div class="ibox">
                    <div class="ibox-title">
                        @ViewData["Title"]
                    </div>
                    <div class="ibox-content">
                        <dl class="row">
                            <dt class="col-sm-2">
                                Game Server
                            </dt>
                            <dd class="col-sm-10" data-testid="ban-file-monitor-details-gameserver">
                                <server-name title="@Model.GameServer.Title"
                                    live-title="@Model.GameServer.LiveTitle"></server-name>
                            </dd>
                            <dt class="col-sm-2">
                                @Html.DisplayNameFor(model => model.FilePath)
                            </dt>
                            <dd class="col-sm-10" data-testid="ban-file-monitor-details-filepath">
                                <ftp-path host="@GetCfg(serverConfigs, Model.GameServerId, "ftp", "hostname")" port="@GetCfgInt(serverConfigs, Model.GameServerId, "ftp", "port", 21)"
                                    path="@Model.FilePath" user="@GetCfg(serverConfigs, Model.GameServerId, "ftp", "username")"
                                    password="@GetCfg(serverConfigs, Model.GameServerId, "ftp", "password")"></ftp-path>
                            </dd>
                            <dt class="col-sm-2">
                                @Html.DisplayNameFor(model => model.RemoteFileSize)
                            </dt>
                            <dd class="col-sm-10" data-testid="ban-file-monitor-details-filesize">
                                @Html.DisplayFor(model => model.RemoteFileSize)
                            </dd>
                            <dt class="col-sm-2">
                                @Html.DisplayNameFor(model => model.LastSync)
                            </dt>
                            <dd class="col-sm-10" data-testid="ban-file-monitor-details-lastsync">
                                @Html.DisplayFor(model => model.LastSync)
                            </dd>
                        </dl>
                    </div>
                    <div class="ibox-footer">
                        <div class="btn-group btn-group-sm" role="group">
                            <a class="btn btn-primary" asp-action="Edit" asp-route-id="@Model.BanFileMonitorId"
                                data-testid="ban-file-monitor-details-edit">
                                <i class="fa-solid fa-pen-to-square" aria-hidden="true"></i> Edit
                            </a>
                            <a class="btn btn-primary" asp-action="Index" data-testid="ban-file-monitor-details-back">
                                <i class="fa-solid fa-arrow-left" aria-hidden="true"></i> Back to List
                            </a>
                        </div>
                    </div>
                </div>
            </div>

        </div>
    </div>

</div>